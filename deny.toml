[graph]
targets = []
all-features = true

[advisories]
# time 0.3.47 fix requires Rust 1.88; transitive dep via ratatui,
# khop never parses RFC 2822 dates from user input
ignore = ["RUSTSEC-2026-0009"]

[licenses]
allow = [
    "Apache-2.0",
    "MIT",
    "MPL-2.0",
    "Unicode-3.0",
    "Zlib",
]
